<UserControl x:Class="Main.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodel="clr-namespace:Main.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <DataTemplate DataType="{x:Type viewmodel:AccountViewModel}">
        <Grid>
            <Label Content="Account View" HorizontalAlignment="Left" Margin="159,78,0,0" VerticalAlignment="Top" />
            <Label Content="{Binding Name}" HorizontalAlignment="Left" Margin="271,257,0,0" VerticalAlignment="Top" />
            <Label Content="{Binding Email}" HorizontalAlignment="Left" Margin="203,211,0,0" VerticalAlignment="Top" />

        </Grid>
    </DataTemplate>
</UserControl>