<UserControl x:Class="Main.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Main.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="SEARCHING IS FUN"></TextBlock>
        <ListView Grid.Row="1" Margin="0 20 0 0 " ItemsSource="{Binding Books}">
            <ListView.View>
              <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Author}"/>
                    <GridViewColumn Header="AvailableCopies" DisplayMemberBinding="{Binding Title}"/>
                    <GridViewColumn Header="Genre" DisplayMemberBinding="{Binding Genre}"/>
                    <GridViewColumn Header="Genre" DisplayMemberBinding="{Binding Price}"/>
                    <GridViewColumn Header="Genre" DisplayMemberBinding="{Binding PublicationDate}"/>
                    <GridViewColumn Header="Genre" DisplayMemberBinding="{Binding Summary}"/>
              </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
